apiVersion: v1
kind: Service
metadata:
  name: waystar-demo
spec:
  selector:
    app: waystar-demo
    harness.io/color: blue
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: LoadBalancer